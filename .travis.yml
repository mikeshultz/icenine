language: python
python:
  - "3.5"
  - "3.6"
install: 
  - pip install .
  - pip install -r requirements.txt
  - pip install -r requirements.dev.txt
script: py.test --cov=icenine
env:
  - BUILDENV="travis-ci"